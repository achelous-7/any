
name: TOOLBOXLAP

on:
  workflow_dispatch:

  jobs:
    setup-rdp-tunnel:
        runs-on: windows-2025

            steps:
                - name: Check out the repository
                      uses: actions/checkout@v2

                          - name: Download and Install Playit
                                run: |
                                        Invoke-WebRequest -Uri "https://github.com/playit-cloud/playit-agent/releases/download/v0.15.26/playit-windows-x86_64-signed.exe" -OutFile "$env:USERPROFILE\playit.exe"
                                                Start-Sleep -Seconds 5  # Give some time for the download to complete

                                                    # Default, optional.
                                                        - name: Enable TS
                                                              run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server'-name "fDenyTSConnections" -Value "Rem  - Desktop"
                                                                  - run: Set-ItemProperty -Path 'HKLM:\System\Current-Tcp' -net\ContrrAuthenticatierv -Value 1
                                                                      - run: Set-LocalUser -Name "runneradmin" -Pass    - run: Set-LocalUser -Name "runneradmin" -Pass    - run: Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "admin@123" -Force)

                                                                          - name: www.toolboxlap.xyz
                                                                            }   env: run: |
                                                                                   _AUTH_KEY: ${{ seilePs.PL"$env:USERPROFILE\playit.exe" -ArgumentList "--secret $env:RPROFILE\playit.exe" -ArgumentList "--secret $env:PLAYIT_AUTH_KEY" -NoNewWindRPROFILE\playit.exe" -NoNewWindow
                                                                                       #  www.toolERPROFILE\playi# Prev -NoNewWindow
                                                                                           #  www.toolboxlap.com
                                                                                               # Prevction Runner Alive
                                                                                                     run: |
                                                                                                       eep th  Start-Sleep - Runner A1600  # Adjuun: |
                                                                                                                 Start-Sleep -Seconds 21600  # Adjust the duration based on your needs
